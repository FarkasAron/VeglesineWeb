# PDF extraction pipeline dependencies
# Install: pip install -r requirements.txt

# Step 01 — download
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0          # faster BS4 parser

# Step 03 — PDF text/image extraction
PyMuPDF>=1.24.0      # import fitz

# Step 03 fallback — OCR for scanned PDFs
pytesseract>=0.3.10
Pillow>=10.0.0

# Step 07 — Supabase import
supabase>=2.4.0
python-dotenv>=1.0.0

# Streamlit launcher (pipeline_app.py)
streamlit>=1.32.0
